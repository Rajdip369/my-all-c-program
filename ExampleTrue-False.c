#include<stdio.h>
int main()
{
    int sunday=0;//user input
    int snowing=1;//printf(Enter 1 if its snowing else 0:)
    //user ans must be either 1 or 0 if(0 || 1)
    printf("%d",snowing==1 || sunday==1);

    return 0;
}

