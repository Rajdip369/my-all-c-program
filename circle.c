#include<stdio.h>
int main()
{ int area,radius;

    printf("Enter the the radius");
    scanf("%d",&radius);

 area=(22/7)*radius*radius;

    printf("area of circle is:%d",area );
 return 0;
}